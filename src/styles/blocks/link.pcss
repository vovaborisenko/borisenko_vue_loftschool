.link {
  display: inline-flex;
  align-items: center;
  color: $color-link;

  &:hover {
    text-decoration: underline;
  }

  &__icon {
    flex: none;
    font-size: 1.61em;

    @include desktop {
      font-size: 1.31em;
    }
  }

  &__icon + &__text {
    margin-left: 16px;
  }
}
